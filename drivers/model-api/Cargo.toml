[package]
name = "portos-model-api"
version.workspace = true
edition.workspace = true

[dependencies]
portos-proto = { path = "../../crates/portos-proto" }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
