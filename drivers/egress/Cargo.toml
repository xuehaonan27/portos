[package]
name = "portos-egress-api"
version.workspace = true
edition.workspace = true

[dependencies]
portos-abi = { path = "../../abi" }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
