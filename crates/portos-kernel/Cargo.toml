[package]
name = "portos-kernel"
version.workspace = true
edition.workspace = true

[dependencies]
# plan-shapes enabled since D41 (G1): the D31 quarantine is lifted. The
# `attach` feature stays off until WP-08.
portos-proto = { path = "../portos-proto" }
portos-rm = { path = "../portos-rm", default-features = false, features = ["plan-shapes"] }
serde = { workspace = true }
serde_json = { workspace = true }
blake3 = { workspace = true }
rusqlite = { workspace = true }
rand = { workspace = true }
regex = { workspace = true }
hex = { workspace = true }
nix = { workspace = true }
